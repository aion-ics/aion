{
  "name": "aion",
  "version": "1.0.0",
  "description": "Aion",
  "main": "index.ts",

  "bin": {
    "aion": "./dist/index.js"
  },

  "scripts": {
      "build": "tsc",
      "start": "node src/index.ts",
     "test": "echo \"Error: no test specified\" && exit 1",
       "pkg": "pkg .",
    "make-exe": "npm run build && pkg . --targets node18-win-x64 --output dist/aion.exe"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.13.4",
    "typescript": "^5.7.3"
  }
}
